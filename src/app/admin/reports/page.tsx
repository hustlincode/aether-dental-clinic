import { UpcomingModule } from "@/components/admin/upcoming-module";

export default function AdminReportsPage() {
  return (
    <UpcomingModule
      title="Reports"
      description="Appointment and revenue reporting with date-range, dentist, and service filters. This module is planned for the next development cycle."
    />
  );
}
